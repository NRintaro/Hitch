<% if current_user.likes.find_by(post_id: post.id) %>
    <%= link_to 'いいね！を取り消す', unlike_path(post.id), method: :delete, remote: true %>
  <% else %>
    <%= link_to 'いいね！', like_path(post.id), method: :post, remote: true %>
<% end %>
